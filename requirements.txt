# Production dependencies
# Dependencies added incrementally as we build features:

# Phase 3: Parquet support
pyarrow>=10.0.0

# Phase 6: HTTP streaming (coming soon)
# httpx>=0.27.0

# Phase 7: CLI (coming soon)
# click>=8.0.0
# rich>=13.0.0
